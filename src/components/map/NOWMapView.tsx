
import React from 'react';
import MapView from './MapView';

// NOWMapView component - a focused entry point for the map functionality
const NOWMapView = () => {
  return <MapView />;
};

export default NOWMapView;
