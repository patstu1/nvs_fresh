import 'package:flutter/material.dart';

class NVSPalette {
	// Core brand colors
	static const Color mint = Color(0xFF00FFC2);
	static const Color neonLime = Color(0xFFCCFF00);

	// Grays & dark mode
	static const Color pureBlack = Color(0xFF000000);
	static const Color surfaceDark = Color(0xFF0D0D0D);
	static const Color lightGray = Color(0xFFBFBFBF);
	static const Color mediumGray = Color(0xFF808080);
	static const Color darkGray = Color(0xFF404040);
	static const Color chromeGray = Color(0xFF1A1A1A);

	// Status colors
	static const Color errorRed = Color(0xFFFF3B30);
	static const Color successGreen = Color(0xFF32D74B);
	static const Color warningYellow = Color(0xFFFFD60A);

	// Transparent overlays
	static const Color overlayDark = Color(0xAA000000);
	static const Color overlayLight = Color(0x55FFFFFF);

	// Profile accents
	static const Color profileMint = mint;
	static const Color profileNeonLime = neonLime;
}
