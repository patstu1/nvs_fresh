import 'package:flutter/widgets.dart';

class PerformanceService {
  static Future<void> initialize() async {
    // No-op for now. Add performance monitoring or custom logic here if needed.
  }

  static Widget wrapWithPerformanceMonitoring(Widget child) {
    // No-op wrapper for now. Add performance overlays or monitoring if needed.
    return child;
  }
}
